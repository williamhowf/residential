@model AnnouncementsModel

@{
    //page title
    ViewBag.Title = T("Admin.ContentManagement.Announcements.AnnouncementsItems.AddNew").Text;
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("Announcement");
}

<form asp-controller="Announcements" asp-action="Create" method="post">
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Admin.ContentManagement.Announcements.AnnouncementsItems.AddNew")
            <small>
                <i class="fa fa-arrow-circle-left"></i>
                <a asp-action="List">@T("Admin.ContentManagement.Announcements.AnnouncementsItems.BackToList")</a>
            </small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.SaveContinue")
            </button>
            @await Component.InvokeAsync("AdminWidget", new { widgetZone = "admin_announcement_details_buttons" })
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdate", Model)
</form>